# CONFIG file
import os, os.path as op

# data file paths
data_dir = "data"
train_dir = op.join(data_dir, "train")
test_dir = op.join(data_dir, "test")
pred_dir = op.join(data_dir, "preds")
score_dir = op.join(data_dir, "scores")
